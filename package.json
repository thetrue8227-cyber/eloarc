{
  "name": "elo-arc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ..",
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"magenta,cyan\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "seed": "cd server && node seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
